{
  "name": "saf-website",
  "version": "0.8.0",
  "description": "Small Animal Facility - IISER Kolkata",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "build": "npm run clean && parcel build src/index.html --no-cache --dist-dir dist --public-url ./"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0",
    "rimraf": "^5.0.0",
    "@parcel/transformer-html": "^2.12.0"
  }
}
